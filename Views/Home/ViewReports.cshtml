@{
    ViewData["Title"] = "Descargar Reporte";
    var publicPath = ViewData["PublicPath"];
    Console.WriteLine($"Ruta p�blica in VR: {publicPath}");
}
<div class="mt-20 flex justify-center">
    <div class="border-2 container flex max-w-[95%] flex-col items-center rounded-lg bg-gray-500 p-5 text-white">
        <h1 class="py-2 text-center text-3xl">
            <strong>Visualizar PDF</strong>
        </h1>

    @if (publicPath == null)
    {
        <p>No hay un reporte disponible para mostrar.</p>
    }
    else
    {
        <div class="container">
            <iframe src="@publicPath" width="100%" height="600px" frameborder="0"></iframe>
        </div>
    }
    </div>
</div>


    
