<!DOCTYPE html>
<html lang="en">
    <head>  
        <meta charset="utf-8" />  
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />  
        <title>@ViewData["Title"] - Reporteadores</title>
        <link href="~/css/output.css" rel="stylesheet">
        <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">  
        <!-- Carga jQuery primero -->  
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>  
        <!-- Carga el CSS de DataTables -->  
        <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css">  
        <!-- Carga el script de DataTables -->  
        <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>  
    </head>

    <body>
        <header class="fixed top-0 z-50 flex w-full bg-white py-2 shadow-md">
            <div class="flex flex-grow">
                <a id="logo" asp-area="" asp-controller="Home" asp-action="Index">
                    <img class="h-10 w-30 py-1 pl-5" src="@Url.Content("~/images/logo.webp")">
                </a>
            </div>

            <!-- Botón de menú hamburguesa siempre visible en móvil -->
            <button id="menu-button" class="block justify-end rounded p-2 text-gray-700 hover:bg-blue-100 lg:hidden" onclick="toggleMenu()">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
            </button>

            <!-- Menú de navegación -->
            <nav id="menu" class="hidden flex-grow flex-col space-y-2 pr-10 lg:flex lg:flex-row lg:space-y-0 lg:space-x-4">
                @if (ViewData["UserActive"] != null)
                {
                    <section class="flex flex-grow">
                        <ul class="flex justify-center">
                            <li><a class="inline-block px-2 py-1 text-[15px] hover:decoration-blue-500 hover:rounded hover:text-white hover:bg-blue-500 hover:underline" asp-area="" asp-controller="Home" asp-action="Reporte">Reportes</a></li>
                            <li><a class="inline-block px-2 py-1 text-[15px] hover:decoration-blue-500 hover:rounded hover:text-white hover:bg-blue-500 hover:underline" asp-area="" asp-controller="Home" asp-action="Nomina">Nomina</a></li>
                            <li><a class="inline-block px-2 py-1 text-[15px] hover:decoration-blue-500 hover:rounded hover:text-white hover:bg-blue-500 hover:underline" asp-area="" asp-controller="Home" asp-action="SoporteReporte">Soporte Técnico</a></li>
                        </ul>
                    </section>
                    <section class="flex flex-grow justify-end">
                        <ul>
                            <li><a class="inline-block px-2 py-1 text-[15px] hover:decoration-blue-500 hover:rounded hover:bg-blue-500 hover:underline hover:text-white" asp-area="" asp-controller="Home" asp-action="Signout">Cerrar Sesión</a></li>
                        </ul>
                    </section>
                }
                else
                {
                    <section class="flex flex-grow">
                        <ul class="justify-center">
                            <li><a class="inline-block px-2 py-1 text-[15px] hover:decoration-blue-500 hover:border-2 hover:rounded hover:text-white hover:bg-blue-500 hover:underline" asp-area="" asp-controller="Home" asp-action="SoporteReporte">Soporte Técnico</a></li>
                        </ul>
                    </section>
                    <section class="flex flex-grow justify-end">
                        <ul>
                            <li><a class="inline-block px-2 py-1 text-[15px] hover:decoration-blue-500 hover:rounded hover:bg-blue-500 hover:underline hover:text-white" href="/">Contacto</a></li>
                        </ul>
                    </section>
                }
            </nav>

        
        
        </header>

        <!-- Script para el menú hamburguesa -->
        <script>
            function toggleMenu() {
                const menu = document.getElementById('menu');
                menu.classList.toggle('hidden');
            }
        </script>

        <main role="main">
            @RenderBody()
            <div id="loadingModal" class="bg-opacity-50 fixed inset-0 hidden bg-black">
                <div class="absolute top-1/2 left-1/2 container -translate-x-1/2 -translate-y-1/2 transform content-center rounded-lg bg-white p-6 text-center shadow-lg">
                    <p class="mb-4 text-lg font-semibold">Cargando, por favor espera...</p>
                    <div class="border-t-4 h-12 w-12 animate-spin rounded-full border-blue-500"></div>
                </div>
            </div>
        </main>

        @if (ViewData["UserActive"] == null)
        {
            <footer class="bg-black py-1 text-center text-white">
                <div class="flex h-full items-center justify-center bg-[#437cb1] py-4">
                    <img class="h-12 w-12" src="~/images/woman-with-headset.webp" alt="Atención vía telefónica">
                </div>
                <div class="h-full items-center bg-[#437cb1]">
                    <p>La atención vía telefónica está disponible de <b>Lunes a Viernes</b> en un horario de <b>9:00 a.m a 2:00 p.m</b> y de <b>4:00 p.m a 6:00 p.m</b> marcando al número telefónico <b>444-151-85-00 Ext.138</b>, en él que atenderemos tus dudas sobre el portal así como brindarte soporte técnico.</p>
                </div>
                <div class="h-full p-4">
                    <p>&copy; 2024 - Reportes ABG | Todos los derechos reservados | GrupoABG</p>
                    <p class="decoration-blue-500 hover:underline hover:text-blue-500"><a asp-area="" asp-controller="Home" asp-action="Privacy">Aviso de Privacidad</a></p>
                </div>
            </footer>
        }
    
        <script src="~/lib/jquery/dist/jquery.min.js"></script>
        @await RenderSectionAsync("Scripts", required: false)

    </body>

</html>
